{
  "name": "apbeauty-backend",
  "version": "1.0.0",
  "description": "Backend de pagos AP Beauty — moneda PEN; Stripe / Mercado Pago vía CHECKOUT_GATEWAY",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --env-file-if-exists=.env --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "stripe": "^17.5.0"
  }
}
